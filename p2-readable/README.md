# Readable Project

Second project for Udacity's React nanodegree. React + Redux application allowing users to submit posts and comments.

## Pre-requisites
- [Node](https://nodejs.org)

## Set-up
1. Run `npm install`
2. Start the server by running `npm run server`
3. Then run `npm run start`